R3 delta review complete — range `0507f2613..46f3385a3e2962bcb425e1f795d265ca34c95703`, source of truth `docs/vscode-v0.8.0-plan.md` @ `2429`. Prior R2 deferred hardening unchanged. Fattie check report not duplicated; no tests/builds run. Read-only.

**No material actionable defects in this delta.**

What was inspected, with evidence:

- Caller restoration (plan rules 3–4, Initial-selection + Panels gates): `workspace-screen.tsx:2301-2311` re-inserts `tryOpenWorkspaceFileInVscode({isVscode: getIsVscode(), location, workspaceDirectory, ...})` before the in-app disposition branch and returns on `true`. Receiver module was unchanged, consistent with PIC's merge-drop diagnosis. Placement is correct: all VS Code opens (any disposition) route native; invalid targets are consumed with the existing `failedOpenFile` toast rather than falling through.
- Helper validity + security: `vscode-file-open.ts:15-45` gates on `isVscode`, normalizes via shared `normalizeWorkspaceFileLocation`/`resolveWorkspaceFilePaths`, consumes null/invalid with `onError`, and forwards only resolved `absolutePath` + `line`/`lineEnd` to `openDesktopTarget({editorId:"vscode-self",...})` with async errors mapped to the same toast. Traversal (`../outside.ts`) resolves null app-side and never reaches the bridge; bridge `editor-commands.ts:34-62` allowlists `vscode-self`, requires workspace path, validates positive lines and `lineEnd >= line`. No new schemes, no `unsafe-eval`, no endpoint/auth change.
- Unit coverage is meaningful (not string-only): `vscode-file-open.test.ts` asserts exact `openTarget` args for `.github/qa-example.ts` lines 3–5, `false` passthrough off-VS Code, and traversal consumed with the existing error. Dot-path regression (`517948bf`) is directly covered.
- E2E validity: `vscode-e2e.mjs` seeds via supported path — real daemon + built `DaemonClient` (`daemon-seed.mjs`, password in-process, `reconnect:false`, `close()` in `finally`), `provider:"mock"` with `mockStreamingAssistantResponse/IntervalMs`, server-id match check, in-webview route navigation to the seeded agent with `open`-param-consumed wait. Asserts streaming→complete SVG labels, fullscreen open/labels/close-detach, native tab + line via `assertNativeEditorLocation`, required screenshots at each success surface, and a terminal CSP-violation fail (`/content security policy|script-src|unsafe-eval/`). `daemon-harness.mjs` change is additive (`environment` spread, dev flags only in the scratch daemon). Docs (`vscode-ci.md`) accurately retire the obsolete skip language.
- PIC's independent real-provider evidence (`qa-example.ts` Ln3 Col1, `final/13-fixed-native-file-line.png`) is noted as complementary, not a substitute; the deterministic mock spec is the repeatable gate.

Gates affected, all satisfied at code level with QA evidence left to Pillow/Fattie runs: native file open + line navigation (restored), Mermaid streaming/fullscreen + CSP, section-rail/composer untouched by this delta. Nothing to adopt; no follow-up assigned by this review.
